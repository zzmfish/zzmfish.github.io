```uml
class JobClient {
    - writeSplits(job, jobSubmitDir): int
    +submitJob(job): RunningJob
}

class JobSubmissionProtocol {
    + getNewJobId(): JobID
    + submitJob(jobName, jobSubmitDir, ts): JobStatus
}

class JobTracker {

}

JobSubmissionProtocol <|-- JobTracker
JobClient o- JobSubmissionProtocol
```